{
  "name": "{{PROJECT_NAME}}",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bxn start --watch",
    "start": "bxn start",
    "build": "tsc"
  },
  "dependencies": {
    "@buildxn/http": "{{HTTP_VERSION}}"
  },
  "devDependencies": {
    "bxn": "{{BXN_VERSION}}",
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0"
  }
}
